import styled from "@emotion/styled";

export const NoCollectionContainer = styled.div(() => {
  return {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
    height: "50%",
    textAlign: "center",
    ">div": {
      textAlign: "center",
    },
  };
});
